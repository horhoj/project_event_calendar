import { FC } from 'react';
import { EventCalendarMain } from '../features/EventCalendar/EventCalendarMain/EventCalendarMain';

export const EventCalendarPage: FC = () => {
  return <EventCalendarMain />;
};
